#include<stdio.h>
#include<string.h>
int main()
{
    char str[50];
    int i,len=0;
    printf("Enter the input string: ");
    gets(str);   //scanf("%[^\n]",str);
    printf("%s\n",str);
}
